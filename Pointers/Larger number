#include <stdio.h>

int main()
{
    int a, b;
    int *p = &a, *q = &b;

    printf("Enter two numbers: ");
    scanf("%d%d", p, q);

    if(*p > *q)
        printf("%d is largest", *p);
    else
        printf("%d is largest", *q);

    return 0;
}
